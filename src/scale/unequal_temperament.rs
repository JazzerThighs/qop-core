use crate::scale::ListedScale;
use lazy_static::lazy_static;

lazy_static! {
    pub static ref UET_SCALES: Vec<ListedScale> = vec![
        ListedScale {
            
            ..Default::default()
        },
    ];
}